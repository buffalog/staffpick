BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Provider] DROP CONSTRAINT [Provider_user_id_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
